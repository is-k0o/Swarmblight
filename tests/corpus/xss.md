# Encoded HTML reflection

A request value appears in an HTML response, but angle brackets are encoded. Reflection proves
that data reached output; it does not prove creation of an executable browser context. Compare the
raw response, parsed DOM, and any supplied execution evidence before escalating an XSS hypothesis.
